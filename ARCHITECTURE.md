# Architecture

Self-contained repo. Plugin source + skills snapshot committed. Bootstrap script renders parameterized config and builds plugin in place. Idempotent. No submodules. No remotes required at runtime.

See [docs/how-it-works.md](docs/how-it-works.md) for the bootstrap flow.
